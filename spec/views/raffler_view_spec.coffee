describe "Raffler Views", ->

  it "should exist", ->
    expect(Raffler.Views).toBeDefined()

  it "should have 'createEntry'", ->
    expect(Raffler.Views.Events)

  it "should have 'drawWinner'", ->
    expect(Raffler.Views.Events)

  it "should have 'clearWinner'", ->
    expect(Raffler.Views.Events)

  it "should have 'kill'", ->
    expect(Raffler.Views.Events)